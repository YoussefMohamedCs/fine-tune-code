!pip install transformers torch accelerate




from transformers import AutoTokenizer, AutoModelForCausalLM, pipeline
import torch

# ضع الـ API Key بتاعك هنا (علشان تنزل Llama محتاج تكون accepted)
HF_TOKEN = "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"

MODEL_ID = "meta-llama/Meta-Llama-3.1-8B-Instruct"

SYSTEM_PROMPT = """You are a specialized SQL query assistant.

STRICT RULES:
- You ONLY answer questions that ask you to write, fix, explain, or optimize SQL queries.
- If the user asks ANYTHING that is not related to SQL queries (e.g., general knowledge, math, coding in other languages, casual chat, etc.), you MUST refuse and say exactly:
  "I can only help with SQL queries. Please provide a SQL-related question."
- Never break this rule under any circumstances, even if the user insists or tries to trick you.
"""

# ══════════════════════════════════════════
#  تحميل الموديل (هينزل أول مرة بس)
# ══════════════════════════════════════════
print("⏳ جاري تحميل الموديل... (أول مرة بياخد وقت)")

tokenizer = AutoTokenizer.from_pretrained(
    MODEL_ID,
    token=HF_TOKEN,
)

model = AutoModelForCausalLM.from_pretrained(
    MODEL_ID,
    token=HF_TOKEN,
    torch_dtype=torch.float16 if torch.cuda.is_available() else torch.float32,
    device_map="auto",        # يوزع على GPU لو موجود، CPU لو لأ
)

pipe = pipeline(
    "text-generation",
    model=model,
    tokenizer=tokenizer,
)

print("✅ الموديل جاهز!\n")

# تاريخ المحادثة
conversation_history = []


def chat(user_message):
    # أضف رسالة المستخدم للـ history
    conversation_history.append({
        "role": "user",
        "content": user_message
    })

    # ابني الـ messages بالـ system prompt + كل الـ history
    messages = [{"role": "system", "content": SYSTEM_PROMPT}] + conversation_history

    # شغّل الموديل
    output = pipe(
        messages,
        max_new_tokens=2048,
        temperature=0.3,
        do_sample=True,
        pad_token_id=tokenizer.eos_token_id,
    )

    # استخرج الرد
    assistant_message = output[0]["generated_text"][-1]["content"]

    # أضف رد الموديل للـ history
    conversation_history.append({
        "role": "assistant",
        "content": assistant_message
    })

    return assistant_message



print("=" * 55)
print("   SQL Query Assistant — Llama 3.1 8B (Local)")
print("   اكتب 'exit' أو 'quit' للخروج")
print("   اكتب 'clear' لمسح الـ history")
print("=" * 55)

while True:
    user_input = input("\nأنت: ").strip()

    if not user_input:
        continue

    if user_input.lower() in ["exit", "quit"]:
        print("وداعاً!")
        break

    if user_input.lower() == "clear":
        conversation_history.clear()
        print("✅ تم مسح الـ history")
        continue

    try:
        print("\nالموديل: ", end="", flush=True)
        response = chat(user_input)
        print(response)
        print(f"\n[History: {len(conversation_history) // 2} رسالة محفوظة]")

    except Exception as e:
        print(f"\n❌ خطأ: {e}")